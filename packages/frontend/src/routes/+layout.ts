// Static site generation settings
// All pages will be prerendered at build time
export const prerender = true;

// Disable SSR - render only on client
// This allows SPA-style routing with the fallback index.html
export const ssr = false;
